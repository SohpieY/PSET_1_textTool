<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbars="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Button to load a text file -->
        <Button
            android:id="@+id/button_load_text_file"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:text="Select Text File"
            android:textAllCaps="false" />

        <!-- Button to load a PDF file -->
        <Button
            android:id="@+id/button_load_pdf_file"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:text="Select PDF File"
            android:textAllCaps="false" />

        <!-- TextView to display the random generated paragraph -->

        <!-- TextView to display text statistics -->

        <TextView
            android:id="@+id/text_view_random_paragraph"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="#EFEFEF"
            android:ellipsize="none"
            android:gravity="start"
            android:inputType="textMultiLine"
            android:padding="8dp"
            android:scrollbars="vertical"
            android:scrollHorizontally="false"
            android:singleLine="false"
            android:text="Paragraph will appear here..."
            android:textColor="#000000"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/text_view_statistics"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:padding="8dp"
            android:background="#EFEFEF"
            android:text="Statistics will appear here..."
            android:textColor="#000000"
            android:textSize="14sp" />

        <!-- Input field for the temperature -->
        <EditText
            android:id="@+id/input_temperature"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:hint="Enter temperature from 0 to 1 (e.g., 0.5)"
            android:inputType="numberDecimal"
            android:padding="8dp"
            android:background="@android:drawable/edit_text"
            android:textColor="#000000"
            android:textSize="14sp" />

        <!-- Input field for the word count -->
        <EditText
            android:id="@+id/word_count"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:hint="Enter the number of words you wanna include (e.g., 100) "
            android:inputType="numberDecimal"
            android:padding="8dp"
            android:background="@android:drawable/edit_text"
            android:textColor="#000000"
            android:textSize="14sp" />

        <!-- Button to generate a random paragraph -->
        <Button
            android:id="@+id/button_generate_paragraph"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Generate Random Paragraph"
            android:textAllCaps="false" />

        <!-- Button to save the statistics and random paragraph as a PDF -->
        <Button
            android:id="@+id/button_save_as_pdf"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Save as PDF"
            android:textAllCaps="false" />

    </LinearLayout>
</ScrollView>